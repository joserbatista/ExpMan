/* global angular */
(function () {
    'use strict';

    angular
        .module('expman')
        .controller('IndexController', Controller);

    function Controller() {
        //var vm = this;

        initController();

        function initController() {
        }
    }
})();
